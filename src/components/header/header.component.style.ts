import styled from '@emotion/styled';

export const HeaderContent = styled('div')({
  position: 'absolute',
  top: 45,
  left: 0,
  width: '100%',
  borderBottom: 1,
  borderColor: 'divider',
  zIndex: 2,
  background: '#e3e3e3',
});
